去元音 remove vowel

int main(){
	string str = "abdhfgebsa";
	string v = "aeiouAEIOU";
	string res = "";
	for (int i = 0; i < str.size(); i++){
		if (v.find(str[i]) != string::npos) continue;
		res += str[i];
	}
	return 0;
}
